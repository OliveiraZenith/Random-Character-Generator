DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
JWT_SECRET="replace-with-strong-secret"
PORT=3000
FRONTEND_BASE_URL="http://localhost:5173/register"
PASSWORD_RESET_URL="http://localhost:5173/register"
PASSWORD_RESET_EXPIRES_MINUTES=30

# Optional: adjust bcrypt cost factor (defaults to 10)
# BCRYPT_ROUNDS=10

# SMTP settings for password recovery emails
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="smtp-user"
SMTP_PASS="smtp-pass"
# Set true if using port 465 / implicit TLS
SMTP_SECURE=false
MAIL_FROM="Reino dos Contos <no-reply@meusite.com>"
